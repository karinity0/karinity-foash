import React from "react";

type Props = {};

const WebWork = (props: Props) => {
  return (
    <section className="w-screen h-screen bg-black text-white">WebWork</section>
  );
};

export default WebWork;
